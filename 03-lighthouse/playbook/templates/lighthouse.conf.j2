server {
    listen {{ lighthouse_port }};
    server_name _;

    root {{ lighthouse_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /api/ {
        proxy_pass http://{{ clickhouse_host }}:8123/;
    }
}
